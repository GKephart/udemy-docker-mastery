docker container run -d  --name mysql -e MYSQL_ALLOW_EMPTY_PASSOWRD=True mysql

#use the -v flag to create a named volume
docker container run -d  --name fluffy-bunny -e MYSQL_ALLOW_EMPTY_PASSOWRD=True -v mysql-db:/var/lib/mysql mysql
